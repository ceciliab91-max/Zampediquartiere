[build]
  command = "npm run build"
  publish = "dist"

# SPA Fallback per Single Page Application (evita errori 404 al refresh delle pagine)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
